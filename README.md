# Preview
<div align="center">
    <img src="preview.png"> 
</div>

## Installation
You just have to import `nymph.yml` in `~/.config/alacritty/alacritty.yml`.
```yml
import:
  - ~/.config/alacritty/nymph.yml
```