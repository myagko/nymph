# Preview
<div align="center">
    <img src="preview.png"> 
</div>

## Instalation
Put `nymph_gtk` folder to the themes directory i.e. `/usr/share/themes/` or `~/.themes/` and change system theme via distribution specific tweak tool.